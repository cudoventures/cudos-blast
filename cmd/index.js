const {
    initCmd
} = require("./init");

const {
    compileCmd
} = require("./compile");

const {
    runCmd
} = require("./run");

module.exports.initCmd = initCmd;
module.exports.compileCmd = compileCmd;
module.exports.runCmd = runCmd;
